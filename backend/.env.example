# Copy this to .env and update values
PORT=5000
# Optional: provide a MongoDB connection string to use Atlas instead of file storage
# MONGODB_URI=mongodb+srv://<user>:<pass>@cluster0.mongodb.net/portfolio?retryWrites=true&w=majority
# Admin secret to protect reset endpoint
ADMIN_SECRET=change-me
# GitHub credentials for contributions endpoint
# Create a GitHub personal access token (no scopes required for public profile data)
# GITHUB_TOKEN=ghp_xxx
# GITHUB_USERNAME=your-github-username